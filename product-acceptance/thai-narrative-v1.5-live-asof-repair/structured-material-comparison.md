# Structured material comparison

Clock A and Clock B resolve the same Owner Known life periods and structured scores. Career, finance, health and opportunity evidence keys are stable, and both dates produce the same current/next material. No relevant age, birthday, life-period or Bangkok-day boundary occurs between 7 and 16 August for this fixture.

The divergence occurs later, during reader-visible variant selection. The pre-repair VM presenter seed uses VM `String.hashCode` values (for example `persistence = 961841110`, `builder = 504794108`, `lagna_aquarius = 804501464`) and resolves to seed `28924034132`. Compiled JavaScript supplies different runtime hashes, changing selected finance and transition variants while leaving structured engine facts intact.

The stable compatibility hash pins those accepted values across runtimes. `ThaiMirrorNarrativeVariants` also replaces runtime-seeded `Object.hash` with a stable composite over the section name and seed. A cautious opportunity sentence is normalized across two equivalent structured forms to preserve frozen 5/5 output without changing the claim.

Post-repair checks retain:

- Owner Known Aquarius `19°19′`
- regression 00:03 Aquarius `9°24′`
- Unknown no-lagna fail-closed behavior
- Web/PDF exact parity 5/5
- claim traceability 170/170
