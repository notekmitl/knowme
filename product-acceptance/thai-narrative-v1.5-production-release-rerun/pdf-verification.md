# PDF semantic and visual verification

No release PDF was generated or downloaded. Preview/Production semantic comparison, page counts, byte sizes, SHA-256 hashes, rendering and every-page visual QA were not run because the pre-deploy gate failed before build.

Accepted R7.1 PDF identities remain immutable, but they are not used as a substitute for release verification.
