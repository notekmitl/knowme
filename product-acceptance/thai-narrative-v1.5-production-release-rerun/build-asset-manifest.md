# Build asset manifest

No V1.5 Web build was created because the mandatory pre-deploy functional gate failed. Therefore there are 0 release assets, no deployable asset hashes, and no source-map/secret/local-path scan to report. This is an explicit not-run record, not an empty passing manifest.
