# PR114 OR4 Truth Correction

Status: **OR4 EVIDENCE TRACE IMPROVED — GENERAL PREDICTION MODEL FAILED — CANDIDATE 0016 PRODUCT CONTENT REJECTED**

OR4 improved evidence ownership and traceability, but it did not establish a usable general prediction model. The OR4 selector/decision engine supports only `mahabhut2537.rem0.saturday`. Its 15-profile run produced reader output for 5 profiles and omitted 10 profiles; exact duplicate occurrences were 10. The former label “robustness pass” described deterministic execution inside that narrow envelope, not general predictive coverage or product-content quality.

Candidate 0016 was an evidence-safe skeleton. It omitted relationship, health, the 12-month horizon and the next life period, and its remaining copy was too thin for a reader-facing report. Owner Product Content Review rejected it. It is not an implementation target or acceptance fixture.

Two validator defects are corrected in OR5:

- `claim.authorityTier` must equal the referenced rule's `authorityTier`.
- Every interval in a composite period such as `42-43|61-62` is parsed and validated. A pipe no longer bypasses period checking.

Research record correction: the statement on PDF page 198 / printed page 176 defines zodiac domicile (`ดวงเกษตร`). It does not describe an annual-chart method. The record is now classified as a concept-only zodiac-domain definition with no annual timing authority.

OR5 replaces the rejected independent-owner product gate with Product Interpretation Contract V1. Historical OR4 evidence owner IDs remain available only for traceability.
