/// User-facing growth opportunity card (pattern-based — not prediction).
class GrowthOpportunity {
  const GrowthOpportunity({
    required this.title,
    required this.description,
  });

  final String title;
  final String description;
}
