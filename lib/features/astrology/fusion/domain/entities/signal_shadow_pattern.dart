/// Shadow depth pattern for a fusion signal type.
class SignalShadowPattern {
  const SignalShadowPattern({
    required this.blindSpot,
    required this.overusePattern,
  });

  final String blindSpot;
  final String overusePattern;
}
