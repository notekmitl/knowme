/// Pattern-based tendency — not transit, timing, or date prediction.
class FutureTendency {
  const FutureTendency({
    required this.title,
    required this.description,
  });

  final String title;
  final String description;
}
