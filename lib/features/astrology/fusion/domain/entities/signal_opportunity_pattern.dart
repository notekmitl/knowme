/// Opportunity pattern for a fusion signal type.
class SignalOpportunityPattern {
  const SignalOpportunityPattern({
    required this.opportunityPattern,
  });

  final String opportunityPattern;
}
