/// Short per-lens contribution for the "Why This Appears" section.
class LensOriginInsight {
  const LensOriginInsight({
    required this.lensId,
    required this.lensTitle,
    required this.summary,
  });

  final String lensId;
  final String lensTitle;
  final String summary;
}
